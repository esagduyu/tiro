{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main Tiro window. The window loads the local Tiro web app over http://127.0.0.1; that page uses no Tauri IPC, so only core defaults are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
